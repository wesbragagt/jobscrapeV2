image: mhart/alpine-node:10

pipelines:
  default:
    - step:
        name: Build and test
        caches:
          - node
        script:
          - chmod +x ./build-test-api.sh
          - ./build-test-api.sh
      